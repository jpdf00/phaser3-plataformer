import{screen}from"@testing-library/jest-dom";const util=require("../scenes/util.js");describe("Sort Array Function",(()=>{const e=util.sortArray([{user:"One",score:1},{user:"Two",score:2},{user:"Three",score:3}]);test("it should sort an array of objects by descending order of the score",(()=>{expect(e[0].score).toBe(3)})),test("it should sort an array of objects by descending order of the score",(()=>{expect(e[1].score).toBe(2)})),test("it should sort an array of objects by descending order of the score",(()=>{expect(e[2].score).toBe(1)}))})),describe("Save Score Function",(()=>{test("it should save the score to the leaderboard",(()=>{util.saveScore("jpdf00",10).then((e=>{expect(e.result).toBe("Leaderboard score created correctly.")}))})),test("it should save the score to the leaderboard",(()=>{util.saveScore("jpdf00",10).then((e=>{expect(e.result).not.toBe("")}))}))})),describe("Get Score Function",(()=>{test("it should save the score to the leaderboard",(()=>{util.getScore().then((e=>{expect(e.result.length).toBeGreaterThan(0)}))})),test("it should save the score to the leaderboard",(()=>{util.getScore().then((e=>{expect(e.result.length).not.toBe(0)}))}))}));